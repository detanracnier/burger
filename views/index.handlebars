<h1>BIGS BURGERS!</h1>

<h2>Ready to Eat!!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      <li>{{this.burger_name}}</li>
      <button data-id="{{this.id}}" class="devour-button">Devour it!</button>
    {{/unless}}
  {{/each}}
</ul>

<h2>In your tum-tums</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      <li>{{this.burger_name}}</li>
    {{/if}}
  {{/each}}
</ul>

<h2>Make a burger!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="newBurger">Burger Name:</label>
    <input type="text" id="newBurger" name="name">
  </div>
  <button class="add-burger-button" type="submit">Add Burger</button>
</form>
